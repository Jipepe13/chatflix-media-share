import { ChatContainer } from "@/components/chat/ChatContainer";

const Chat = () => {
  return (
    <div className="min-h-screen bg-background">
      <ChatContainer />
    </div>
  );
};

export default Chat;